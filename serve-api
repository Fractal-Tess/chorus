#!/usr/bin/env sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$SCRIPT_DIR"
exec "$SCRIPT_DIR/.venv/bin/uvicorn" tts_api:app --host "${TTS_HOST:-127.0.0.1}" --port "${TTS_PORT:-8000}" "$@"
